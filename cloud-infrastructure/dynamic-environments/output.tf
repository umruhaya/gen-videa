# Outputs are used to Programmatically create a workflow as in piping output of one action to another
# Will be used later on as we progress (not needed for now)

# output "pubsub_topic_name" {
#   value = google_pubsub_topic.example.name
# }

# output "pubsub_subscription_name" {
#   value = google_pubsub_subscription.example.name
# }